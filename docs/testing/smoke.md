# Manual smoke (after install)

- [ ] `/whoami` returns your Telegram user id
- [ ] Text message gets one final agent reply
- [ ] `/new` starts a fresh session
- [ ] `/stop` aborts an in-flight turn
- [ ] While busy, a second message steers (follow-up) instead of starting a parallel turn
- [ ] `/sethome` + `/home` bind and list a home
- [ ] With TTS + `/voice on`, reply arrives as a voice note when `ffmpeg` is available
- [ ] `messenger_ask` shows inline buttons; choosing one continues the agent
