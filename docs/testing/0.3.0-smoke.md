# 0.3.0 smoke checklist (issue #7)

## Automated

```bash
npm test
node scripts/smoke-http.mjs
```

- [ ] `npm test`
- [ ] `GET /dsh-messenger-gateway/status` — running, telegram, pairingPending
- [ ] `client.js` loads with `__ModuleLoader__.load`
- [ ] Commands include `/model` `/status` (Telegram menu)

## Manual Telegram

- [ ] Private: text turn → progress «Думаю…» → delete → one final reply
- [ ] Reaction 👀 while processing (if enabled)
- [ ] `/status`, `/model`, `/model provider model`
- [ ] Group: ignore plain chat; respond to @bot / reply / command
- [ ] Pairing pending list in Plugins settings; Approve/Reject
- [ ] Animated/video sticker downloads (or clear text note)
- [ ] Reply-quote context in agent prompt
- [ ] Webhook (optional): set transport=webhook + public URL + secret; stop poll

Streaming stays off.
